plugins {
    id 'net.neoforged.moddev' version '2.0.107'
}

version = '1.0.0'
group = 'com.holyswap'

base { archivesName = 'holyswap-neoforge-1.21.5' }

java.toolchain.languageVersion = JavaLanguageVersion.of(21)

neoForge {
    version = '21.5.98'
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}
